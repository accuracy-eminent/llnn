# llnn

Low-level neural network. A simple neural network written in C, designed to be easy to understand for educational purposes.

## How to build

Run `make` and then `build/llnn` to run the neural network, by default it loads an iris classification dataset and trains off of it.

To run the unit tests, run `make` and then run `build/llnn_test` to run the tests.